model Category {
    id          String  @id @default(cuid())
    name        String  @unique
    description String?
    logo        String

    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
    products  Product[]
    
    @@map("categories")
}
